<div class="container">
  <div class="row">
    <div class="col-md-6 col-md-offset-3">
      <div class="well">  
        <h1 class="text-center">Zaloguj się</h1>
        <p>Zaloguj się do serwisu wykorzystując swój adres email oraz hasło. Po zalogowaniu będziesz miał możliwość utworzenia/edycji profilu i sprawdzenia najnowszych ocen.</p>
  
          <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
            <div class="form-group">
              <%= f.label :email %><br />
              <%= f.email_field :email, autofocus: true, class: 'form-control' %>
            </div>
          
            <div class="form-group">
              <%= f.label 'Hasło' %><br />
              <%= f.password_field :password, autocomplete: "off", class: 'form-control' %>
            </div>
          
            <% if devise_mapping.rememberable? -%>
              <div class="form-group">
                <%= f.check_box :remember_me %>
                <%= f.label 'Zapamiętaj mnie' %>
              </div>
            <% end -%>
          
            <div class="form-group">
              <%= f.submit "Zaloguj", class: 'btn btn-primary' %>
            </div>
          <% end %>
          
          <%= render "devise/shared/links" %>
          </div>
      </div>    
    </div>
</div>