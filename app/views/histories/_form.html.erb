<%= form_for(@history) do |f| %>
  <% if @history.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@history.errors.count, "error") %> prohibited this history from being saved:</h2>

      <ul>
      <% @history.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :temat %><br>
    <%= f.text_field :temat, class: 'form-control' %>
  </div>
  <div class="form-group">
    <%= f.label :data %><br>
    <%= f.date_select :data, class: 'form-control' %>
  </div>
  <div class="form-group">
    <%= f.label :ocena %><br>
    <%= f.number_field :ocena, class: 'form-control' %>
  </div>
  <div class="form-group">
    <%= f.submit 'Zapisz', class: 'btn btn-success' %>
  </div>
<% end %>
